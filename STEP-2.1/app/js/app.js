import ReactDOM from 'react-dom';
import AppRoot from './components/AppRoot.jsx';

// Element Application Render into 
var app = document.getElementById('application')

ReactDOM.render(AppRoot, app);
